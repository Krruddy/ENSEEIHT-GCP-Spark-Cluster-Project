---
- name: Update apt cache
  apt:
    update_cache: yes

- name: Install essential system packages
  apt:
    name:
      - vim
      - curl
      - wget
      - unzip
      - net-tools
      - htop
      - git
      - python3-pip
      - software-properties-common
    state: present