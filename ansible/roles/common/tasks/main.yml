---
- name: Install essential system packages
  apt:
    name:
      - vim
      - curl
      - wget
      - unzip
      - net-tools
      - htop
      - git
      - software-properties-common
    state: present
    update_cache: yes
